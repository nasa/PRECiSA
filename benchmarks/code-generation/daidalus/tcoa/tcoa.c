// This file is automatically generated by PRECiSA 

#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
#include<stdbool.h>
#include"precisa_prelude.c"




/*@
axiomatic real_function_tcoa {
logic real tcoa (real sz, real vz) =
((sz * vz) < 0) ? (-((sz / vz))) : (0);
}
*/


/*@
axiomatic fp_function_tcoa {
logic double fp_tcoa (double sz_double, double vz_double) =
(Dmul(sz_double, vz_double) < (double) (0)) ? (Dneg(Ddiv(sz_double, vz_double))) : ((double) (0));
}
*/


/*@ axiomatic tcoa_trans {
predicate tcoa_stable_paths (real sz, real vz, double sz_double, double vz_double) =
((((vz != 0) && ((sz * vz) < 0)) && ((vz_double != (double) (0)) && (Dmul(sz_double, vz_double) < (double) (0)))) || (! (((sz * vz) < 0)) && ! ((Dmul(sz_double, vz_double) < (double) (0)))));
}
*/


/*@
requires (0 <= E_0_double) ;
assigns \nothing;

behavior structure:
ensures ((\result.isValid && (((((\is_finite(\result.value) && \is_finite(sz_double)) && \is_finite(vz_double)) && \is_finite(E_0_double)) && \is_finite(Dmul(sz_double, vz_double))) && \is_finite((double) (0))))
         ==> ((\result.value == fp_tcoa(sz_double, vz_double)))) ;

behavior stable_paths:
ensures ((\result.isValid && (((((\is_finite(\result.value) && \is_finite(sz_double)) && \is_finite(vz_double)) && \is_finite(E_0_double)) && \is_finite(Dmul(sz_double, vz_double))) && \is_finite((double) (0))))
         ==> (\forall real sz, real vz; ((\abs(Dmul(sz_double, vz_double) - (sz * vz)) <= E_0_double
                                          ==> (tcoa_stable_paths(sz, vz, sz_double, vz_double)))))) ;
*/
struct maybeDouble tcoa_double (double sz_double, double vz_double, double E_0_double) {
  struct maybeDouble res;
  if ((sz_double * vz_double) < - (E_0_double))
  { res = someDouble(- ((sz_double / vz_double)));
  } else { if ((sz_double * vz_double) >= E_0_double)
           { res = someDouble((double)(0));
           } else { res = noneDouble();
           }
  }
  return res;
}


/*@
ensures (\forall real sz, real vz; (((((1 <= sz) && (sz <= 1000)) && ((1 <= vz) && (vz <= 1000))) && ((\result.isValid && (\result.isValid && ((((\is_finite(\result.value) && \is_finite(sz_double)) && \is_finite(vz_double)) && \is_finite(Dmul(sz_double, vz_double))) && \is_finite((double) (0))))) && (\abs(sz_double - sz) <= ulp_dp(sz)/2 && \abs(vz_double - vz) <= ulp_dp(vz)/2))))
         ==> ((\abs((\result.value - tcoa(sz, vz))) <= 0x1.86e00000000c7p-39))) ;
*/
struct maybeDouble tcoa_num (double sz_double, double vz_double) {
  return tcoa_double (sz_double, vz_double, 0x1.7a00000000001p-33);
}


int main () { return 0; }