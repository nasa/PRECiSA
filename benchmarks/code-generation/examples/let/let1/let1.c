// This file is automatically generated by PRECiSA 

#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
#include<stdbool.h>
#include"precisa_prelude.c"




/*@
axiomatic real_function_g {
logic real g (real X) =
\let Z = 5 ;
\let Y = (X + Z) ;
((Y + 3));
}
*/


/*@
axiomatic fp_function_g {
logic double fp_g (double X_double) =
\let Z = 5 ;
\let Y_double = Dadd(X_double, (double) (Z)) ;
(Dadd(Y_double, (double) (3)));
}
*/


/*@ axiomatic g_trans {
predicate g_stable_paths (real X, double X_double) =
(\true && \true);
}
*/


/*@
assigns \nothing;

behavior structure:
ensures (\result.isValid
         ==> ((\result.value == fp_g(X_double)))) ;

behavior stable_paths:
ensures (\result.isValid
         ==> (\forall real X; ((\true
                                ==> (g_stable_paths(X, X_double)))))) ;
*/
struct maybeDouble g_double (double X_double) {
  struct maybeDouble res;
  int Z = 5;
  double Y_double = (X_double + (double)(Z));
  res = someDouble((Y_double + (double)(3)));
  return res;
}


/*@
ensures (\forall real X; ((((0 <= X) && (X <= 60)) && (\result.isValid && \abs(X_double - X) <= ulp_dp(X)/2)))
         ==> ((\abs((\result.value - g(X))) <= 0x1.4p-46))) ;
*/
struct maybeDouble g_num (double X_double) {
  return g_double (X_double);
}


int main () { return 0; }