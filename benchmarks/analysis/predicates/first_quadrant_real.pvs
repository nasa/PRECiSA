% This file is automatically generated by PRECiSA 

first_quadrant_real: THEORY
BEGIN
IMPORTING first_quadrant

quadrant_real(r_X: real, r_Y: real): int  =
IF ((0 <= r_X) AND (0 <= r_Y))
THEN 1
ELSIF ((r_X <= 0) AND (0 <= r_Y))
THEN 2
ELSIF (r_X <= 0)
THEN 3
ELSE 4
ENDIF

first_quadrant_real(r_X: real, r_Y: real): bool  =
(quadrant(r_X, r_Y) = 1)

test_real(r_X: real, r_Y: real): int  =
IF first_quadrant(r_X, r_Y)
THEN 1
ELSE 2
ENDIF

END first_quadrant_real