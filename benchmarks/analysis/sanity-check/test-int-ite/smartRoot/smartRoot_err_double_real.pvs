% This file is automatically generated by PRECiSA 

smartRoot_err_double_real: THEORY
BEGIN
IMPORTING smartRoot_err_double

smartRoot_real(r_A: real, r_B: real, r_C: real): real  = LET r_D:real=sqrt(((r_B * r_B) - ((r_A * r_C) * 4)))
                                                         IN IF (((r_B * r_B) - (r_A * r_C)) > 10)
                                                            THEN IF (r_B > 0)
                                                                 THEN ((r_C * 2) / -((r_B + r_D)))
                                                                 ELSE ((r_D - r_B) / (r_A * 2))
                                                                 ENDIF
                                                            ELSE ((r_D - r_B) / (r_A * 2))
                                                            ENDIF
END smartRoot_err_double_real