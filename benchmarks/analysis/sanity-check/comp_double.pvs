comp_double: THEORY
BEGIN
IMPORTING float@aerr754dp

h (A,B : unb_double): unb_double = Dadd(A,B)

g (Z: unb_double): unb_double = h(Dabs(X),Dsqrt(X)) 

f (X, Y: unb_double): unb_double = Dmul(g(X), Y)

END comp_double
