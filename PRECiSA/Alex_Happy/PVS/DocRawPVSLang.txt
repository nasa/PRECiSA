The Language RawPVSLang
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of RawPVSLang==

===Literals===
Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


Integer literals //Integer// are nonempty sequences of digits.




Id literals are recognized by the regular expression
`````letter (["?@_"] | digit | letter)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in RawPVSLang are the following:
  | ``AND`` | ``ARRAY`` | ``BEGIN`` | ``ELSE``
  | ``ELSIF`` | ``END`` | ``ENDIF`` | ``FALSE``
  | ``FUNCTION`` | ``IF`` | ``IMPORTING`` | ``IN``
  | ``LAMBDA`` | ``LET`` | ``NOT`` | ``OR``
  | ``SUBRANGE`` | ``THEN`` | ``THEORY`` | ``TRUE``
  | ``TYPE`` | ``WITH`` | ``below`` | ``for``
  | ``for_down`` | ``lambda`` | ``list`` |

The symbols used in RawPVSLang are the following:
  | , | = | : | :=
  | ( | ) | /= | <
  | <= | > | >= | +
  | - | * | / | [
  | ] | ^ | ` | (#
  | #) | [# | #] | ->
  | | |  |  |

===Comments===
Single-line comments begin with %.There are no multiple-line comments in the grammar.

==The syntactic structure of RawPVSLang==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //[Id]// | -> | //Id//
  |  |  **|**  | //Id// ``,`` //[Id]//
  | //ElsIf// | -> | ``ELSIF`` //Expr// ``THEN`` //Expr//
  | //[ElsIf]// | -> | //ElsIf//
  |  |  **|**  | //ElsIf// //[ElsIf]//
  | //LetElem// | -> | //Id// ``=`` //Expr//
  |  |  **|**  | //Id// ``:`` //Type// ``=`` //Expr//
  | //[LetElem]// | -> | //LetElem//
  |  |  **|**  | //LetElem// ``,`` //[LetElem]//
  | //RecordElem// | -> | //Id// ``:=`` //Expr//
  | //[RecordElem]// | -> | //RecordElem//
  |  |  **|**  | //RecordElem// ``,`` //[RecordElem]//
  | //LambdaKeyWord// | -> | ``LAMBDA``
  |  |  **|**  | ``lambda``
  | //LambdaExpr// | -> | //LambdaKeyWord// ``(`` //Id// ``:`` ``SUBRANGE`` ``(`` //Expr// ``,`` //Expr// ``)`` ``,`` //Id// ``:`` //Type// ``)`` ``:`` //Expr//
  | //[Expr]// | -> | //Expr//
  |  |  **|**  | //Expr// ``,`` //[Expr]//
  | //Expr// | -> | //Expr1//
  |  |  **|**  | ``LET`` //[LetElem]// ``IN`` //Expr//
  | //Expr1// | -> | //Expr2//
  |  |  **|**  | //Expr1// ``OR`` //Expr2//
  | //Expr2// | -> | //Expr3//
  |  |  **|**  | //Expr2// ``AND`` //Expr3//
  | //Expr3// | -> | //Expr4//
  |  |  **|**  | ``NOT`` //Expr4//
  | //Expr4// | -> | //Expr5//
  |  |  **|**  | //Expr5// ``=`` //Expr5//
  |  |  **|**  | //Expr5// ``/=`` //Expr5//
  |  |  **|**  | //Expr5// ``<`` //Expr5//
  |  |  **|**  | //Expr5// ``<=`` //Expr5//
  |  |  **|**  | //Expr5// ``>`` //Expr5//
  |  |  **|**  | //Expr5// ``>=`` //Expr5//
  | //Expr5// | -> | //Expr6//
  |  |  **|**  | //Expr5// ``+`` //Expr6//
  |  |  **|**  | //Expr5// ``-`` //Expr6//
  | //Expr6// | -> | //Expr7//
  |  |  **|**  | //Expr6// ``*`` //Expr7//
  |  |  **|**  | //Expr6// ``/`` //Expr7//
  |  |  **|**  | //Expr6// ``WITH`` ``[`` //Expr// ``:=`` //Expr// ``]``
  | //Expr7// | -> | //Expr8//
  |  |  **|**  | ``-`` //Expr8//
  | //Expr8// | -> | //Expr9//
  |  |  **|**  | //Expr9// ``^`` //Expr8//
  | //Expr9// | -> | //Expr10//
  |  |  **|**  | ``IF`` //Expr// ``THEN`` //Expr// ``ELSE`` //Expr// ``ENDIF``
  |  |  **|**  | ``IF`` //Expr// ``THEN`` //Expr// //[ElsIf]// ``ELSE`` //Expr// ``ENDIF``
  |  |  **|**  | ``for`` ``(`` //Expr// ``,`` //Expr// ``,`` //Expr// ``,`` //LambdaExpr// ``)``
  |  |  **|**  | ``for_down`` ``(`` //Expr// ``,`` //Expr// ``,`` //Expr// ``,`` //LambdaExpr// ``)``
  | //Expr10// | -> | //Expr11//
  |  |  **|**  | //Id// ````` //Integer//
  |  |  **|**  | //Id// ````` //Id//
  |  |  **|**  | //Id// ``(`` //[Expr]// ``)`` ````` //Integer//
  |  |  **|**  | //Id// ``(`` //[Expr]// ``)`` ````` //Id//
  |  |  **|**  | ``(#`` //[RecordElem]// ``#)``
  |  |  **|**  | ``(`` //[Expr]// ``)``
  |  |  **|**  | //Id// ``(`` //[Expr]// ``)``
  |  |  **|**  | //Id//
  |  |  **|**  | //Integer//
  |  |  **|**  | //Double//
  |  |  **|**  | ``TRUE``
  |  |  **|**  | ``FALSE``
  | //Expr11// | -> | ``(`` //Expr// ``)``
  | //FieldDecls// | -> | //Id// ``:`` //Type//
  | //[Type]// | -> | //Type//
  |  |  **|**  | //Type// ``,`` //[Type]//
  | //[FieldDecls]// | -> | //FieldDecls//
  |  |  **|**  | //FieldDecls// ``,`` //[FieldDecls]//
  | //Type// | -> | //Id//
  |  |  **|**  | //Id// ``(`` //Integer// ``,`` //Integer// ``)``
  |  |  **|**  | ``below`` ``(`` //Expr// ``)``
  |  |  **|**  | ``[#`` //[FieldDecls]// ``#]``
  |  |  **|**  | ``[`` //[Type]// ``]``
  |  |  **|**  | ``ARRAY`` ``[`` //[Type]// ``->`` //Type// ``]``
  |  |  **|**  | ``FUNCTION`` ``[`` //[Type]// ``->`` //Type// ``]``
  |  |  **|**  | ``[`` //[Type]// ``->`` //Type// ``]``
  |  |  **|**  | ``list`` ``[`` //Type// ``]``
  | //[Arg]// | -> | //Arg//
  |  |  **|**  | //Arg// ``,`` //[Arg]//
  | //Arg// | -> | //[Id]// ``:`` //Type//
  |  |  **|**  | //[Id]// ``:`` //Type// ``|`` //Expr//
  | //Args// | -> | //[Arg]//
  |  |  **|**  | //[Id]//
  | //[Decl]// | -> | //Decl//
  |  |  **|**  | //Decl// //[Decl]//
  | //Decl// | -> | //Id// ``:`` ``TYPE`` ``=`` //Type//
  |  |  **|**  | //Id// ``(`` //Args// ``)`` ``:`` //Type// ``=`` //Expr//
  |  |  **|**  | //Id// ``:`` //Type// ``=`` //Expr//
  | //Imp// | -> | ``IMPORTING`` //[Id]//
  | //Program// | -> | //Id// ``:`` ``THEORY`` ``BEGIN`` //Imp// //[Decl]// ``END`` //Id//
  |  |  **|**  | //Id// ``:`` ``THEORY`` ``BEGIN`` //[Decl]// ``END`` //Id//



%% File generated by the BNF Converter (bnfc 2.9.6.1).
